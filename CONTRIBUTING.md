# 작업 규칙

혼자 개발하지만 협업 프로젝트와 같은 흐름으로 진행한다.
기록이 남아야 나중에 왜 그렇게 만들었는지 되짚을 수 있다.

## 개인정보 — 가장 중요

**이 저장소는 공개(Public)다. 원생 개인정보가 들어가면 전 세계에 공개된다.**
게다가 한 번 커밋하면 나중에 지워도 git 히스토리에 남아 복구할 수 있다.

지켜야 할 것

- **엑셀·CSV 파일을 저장소 안에 두지 않는다.** `.gitignore`로 막아뒀지만 애초에 넣지 않는다.
  가져오기 스크립트는 저장소 밖 경로(`~/Downloads/...`)를 인자로 받는다
- **시드·테스트 데이터에 실명을 쓰지 않는다.** 홍길동·성춘향처럼 실명이 아님이 명백한
  이름만 쓴다. 실제 명부를 보고 작업하다 보면 이름이 손에 붙어서 그대로 옮겨 적기 쉽다
- **연락처·주소·납부 금액을 예시로 쓰지 않는다.** `010-0000-0000` 같은 명백한 더미만
- 커밋 전 `git diff --cached`로 무엇이 올라가는지 눈으로 확인한다

## 브랜치

| 브랜치            | 역할                                            |
| ----------------- | ----------------------------------------------- |
| `main`            | 학원에서 실제로 돌아가는 버전. 배포된 것만 올라감 |
| `develop`         | 기본 브랜치. 작업이 모이는 곳                    |
| `<타입>/#<이슈>-<설명>` | 작업 브랜치. `develop`에서 따서 `develop`으로 PR |

```
main ──────────────────────●──────────────────●     배포
                          ╱                  ╱
develop ──●────●────●────●────●────●────●───●       개발 통합
           ╲    ╲         ╱     ╲         ╱
            feat/#3   feat/#4   feat/#5              작업
```

작업 브랜치 예시

```
feat/#3-excel_import
fix/#12-attendance_date
docs/#1-convention
```

## 커밋 메시지

```
[<타입>/#<이슈>] 한글로 무엇을 했는지
```

예시

```
[feat/#3] 엑셀 원생 명부 파싱 함수 추가
[fix/#12] 출결 날짜가 UTC로 하루 밀리는 문제 수정
```

## 타입

| 타입    | 쓰는 때                          |
| ------- | -------------------------------- |
| `feat`  | 새 기능                          |
| `fix`   | 버그 수정                        |
| `mod`   | 기능 변경, 리팩터링              |
| `chore` | 설정, 패키지, 빌드               |
| `docs`  | 문서                             |
| `del`   | 파일·코드 삭제                   |

## 두 저장소를 걸치는 작업

API를 만들고 화면을 붙이는 일은 저장소가 나뉘므로 **이슈를 각각 만든다.**
서로의 이슈를 본문에 적어 두면 나중에 따라가기 쉽다.

```
Inspire-academy-project/academy-backend#3
```

먼저 서버 이슈를 닫고, 그다음 화면 이슈를 연다.
API가 없는 상태에서 화면부터 만들면 응답 형태를 두 번 고치게 된다.

## PR 올리기 전 확인

```bash
pnpm lint
pnpm build
```

빌드까지 통과해야 한다. `pnpm dev`만 되고 `pnpm build`에서 깨지는 경우가 있다.

화면은 **로딩 중·에러·데이터 없음** 세 상태를 모두 눈으로 확인한다.
API 서버(`academy-backend`)를 함께 띄워야 한다.
